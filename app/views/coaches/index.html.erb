<%= render 'shared/jumbotron', :image => 'hero-paddlers.jpg', :text => ['PNA', 'Coaches'] %>
<div class="container body-copy">
  <div class="row">
    <div class="col-md-2" data-hook="sidebar">
      <!-- Left Sidebar -->

    </div><!-- ./sidebar -->
    
    <div class="col-md-7" data-hook="content">
      <section id="rco">
        <h2>Regional Coaching Officers</h2>
        <address>
          <strong>North East RCO</strong><br />
          Steven Maynard<br />
          Adirondack Experience<br />
          Plattsburgh State University<br />
          101 Broad Street<br />
          Plattsburgh, NY 12901<br />
          <a href="mailto:Steve@BCUNA.com" target="_blank">Steve@BCUNA.com</a>
        </address>
        <address>
          <strong>South East RCO</strong><br />
          Marsha Henson<br />
          <a href="mailto:Marsha@BCUNA.com" target="_blank">Marsha@BCUNA.com</a><br />
          Phone (912) 663-3169<br />
        </address>
        <address>
          <strong>Central US RCO</strong><br />
          Alec Bloyd-Peshkin<br />
          <a href="mailto:Alec@BCUNA.com" target="_blank">Alec@BCUNA.com</a><br />
          phone (708) 628-4598<br />
        </address>
        <address>
          <strong>West Coast RCO</strong><br />
          John Wallum<br />
          <a href="mailto:John@BCUNA.com" target="_blank">John@BCUNA.com</a><br />
          phone (360) 699-6447<br />
        </address>

        <p>The RCO is your primary source of information. If the RCO can’t immediately answer your question, the RCO will find out, or direct you to the person who will be able to provide an answer.</p>
        <p>All forms and other paperwork should be sent to PNA. Questions as to the status of paperwork in process should be directed to the PNA administrator.</p>
      </section>
      
      
      
        
      <% @coaches.each do |local,coaches| %>
        <section id="<%= local.name.to_sym %>">
          <h2><%= local.name %></h2>
          <% coaches.each do |coach| %>
            <address>
              <strong><%= "#{coach.first_name} #{coach.last_name}" %></strong><br />
              <small><em><%= coach.qualifications.map { |q| q.award.name }.join(', ') %></em></small><br />
              
              <%= "#{coach.addresses.first.city}, #{coach.addresses.first.state.nil? ? coach.addresses.first.country.name : coach.addresses.first.state.name}" %><br />
              
              <% unless coach.email_addresses.empty? %>
                <%= link_to coach.email_addresses.first.address, "mailto:#{coach.email_addresses.first.address}" %><br />
              <% end %>
              
              <% unless coach.centers.empty? %>
                <% coach.centers.each do |center| %>
                  <% if center.url %>
                    <%= link_to center.url, center.url, :target => "_blank"  %><br />
                  <% end %>
                  
                  <% if center.phone_numbers.any? %>
                    <%= center.phone_numbers.first.number %><br />
                  <% end %>
                  
                <% end %>
              <% end %>
            </address>
          <% end %>
        </section>
      <% end %>
    </div>
    
    <div class="col-md-2 col-md-offset-1" data-hook="sidenav">
      <!-- right side nav -->
      <div class="sidenav hidden-sm hidden-xs scrollspy" data-spy="affix" data-offset-top="276" data-offset-bottom="360">
        <ul class="nav">
          <li><a href="#rco">Regional Coaching Officers</a></li>
          <% @coaches.each do |l,c| %>
            <li><a href="#<%= l.name.to_sym %>"><%= l.name %></a></li>
          <% end %>
        </ul>
      </div>
      <!-- end right side nav -->
    </div>      
  </div>
</div>  